return {
  "folke/snacks.nvim",
  opts = {
    -- Disable animated scrolling (from your previous config)
    scroll = { enabled = false },
    -- Keep notifications visible longer
    notifier = {
      timeout = 10000, -- 10 seconds instead of default 5
    },
  },
}
